"""The canonicalization layer: ordered, pure text-to-text filters.

Imports only the standard library, its vendored data, and `nbc.schema` — it must stay usable
in front of any classifier, with no model dependency of its own.
"""
